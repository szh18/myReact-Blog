{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "command": "cat ~/.claude/skills/new-post.md"
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write",
        "command": "if echo \"$CLAUDE_TOOL_PATH\" | grep -q 'content/.*\\.md$'; then echo '📝 检测到文章变更，自动运行测试...'; cd \"$CLAUDE_PROJECT_DIR\" && source ~/.nvm/nvm.sh && nvm use 22.12.0 > /dev/null 2>&1 && npx vitest run 2>&1 | tail -5; fi"
      }
    ]
  }
}
